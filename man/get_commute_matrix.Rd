% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/commute.R
\name{get_commute_matrix}
\alias{get_commute_matrix}
\title{Compute a block-by-school commute-time matrix (seconds)}
\usage{
get_commute_matrix(shp, schools, profile, server, src_chunk, dst_chunk)
}
\arguments{
\item{shp}{An \code{sf} with one row per unit (or a \code{redist_map} whose underlying \code{sf} is used).}

\item{schools}{An integer vector (1-based) of row indices for school units in \code{shp}.}

\item{profile}{OSRM profile, e.g., \code{"car"}}

\item{server}{Base URL of the OSRM server (e.g., \code{"http://127.0.0.1:5000"}).}

\item{src_chunk}{Chunk size for source rows when calling OSRM.}

\item{dst_chunk}{Chunk size for destination rows when calling OSRM.}
}
\value{
A numeric matrix with dimensions \verb{nrow(shp) × length(schools)} of commute times in seconds.
}
\description{
Compute a block-by-school commute-time matrix (seconds)
}
